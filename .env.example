# Kopiere zu ".env" und trage deinen Key ein (nur für LOKALE Tests).
# .env ist per .gitignore vom Commit ausgeschlossen.

GLM_API_KEY=hier-deinen-glm-key-einsetzen

# Optional
GLM_MODEL=glm-5.2
SEARCH_RECENCY=oneWeek        # oneDay | oneWeek | oneMonth | oneYear | noLimit
RESULTS_PER_SOURCE=6
